{
  "name": "funarcade",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node api/server.js",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^6.10.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
